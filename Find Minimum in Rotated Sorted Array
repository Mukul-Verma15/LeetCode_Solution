class Solution {
public:
    int findMin(vector<int>& nums) {
        int mini=nums[0];
        int size=nums.size();

        for(int i=0;i<size;i++){
            mini=min(mini,nums[i]);
        }
        return mini;
    }
};
