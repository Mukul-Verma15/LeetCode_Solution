class Solution {
public:
    int findMin(vector<int>& nums) {
        int size=nums.size();
        int mini=nums[0];

        for(int i=0;i<size;i++){
            mini=min(mini,nums[i]);
        }
        return mini;
    }
};
